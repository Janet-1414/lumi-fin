services:
  - type: web
    name: lumi-fin
    runtime: python
    rootDir: backend
    buildCommand: pip install uv && uv sync --extra dev
    startCommand: uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0